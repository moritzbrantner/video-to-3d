name: Fix mesh footprint validation

on:
  push:
    branches:
      - feat/dense-mesh-foundation

permissions:
  contents: write

jobs:
  fix:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8
        with:
          ref: feat/dense-mesh-foundation
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt,clippy
      - name: Apply mesh footprint fix
        run: python scripts/fix_mesh_footprint.py
      - name: Validate mesh footprint fix
        run: |
          cargo fmt --all
          cargo test -p video-to-3d-core
          cargo clippy -p video-to-3d-core --all-targets -- -D warnings
      - name: Commit fix and remove temporary helpers
        shell: bash
        run: |
          rm .github/workflows/fix-mesh-footprint.yml
          rm scripts/fix_mesh_footprint.py
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Reject fused mesh vertices outside original grid footprints"
          git push origin HEAD:feat/dense-mesh-foundation
