{
  "name": "video-to-3d",
  "private": true,
  "packageManager": "bun@1.4.0",
  "workspaces": ["apps/*"],
  "scripts": {
    "benchmark:wasm-boundary": "WASM_FEATURES=boundary-benchmark bash scripts/build-wasm.sh && bun scripts/benchmark-wasm-boundary.ts",
    "build": "bun run wasm:build && bun --cwd apps/web build",
    "build:pages": "GITHUB_PAGES=true bun run build",
    "cargo:check": "cargo check --workspace",
    "check": "bun run typecheck && bun run test:learned-provider-contract && cargo fmt --check && cargo clippy --workspace --all-targets -- -D warnings && cargo test --workspace && bun run build && bun run test:browser-contract && bun run test:video-sampling-contract",
    "dev": "bun run wasm:build && bun --cwd apps/web dev",
    "tauri:build": "bun run wasm:build && bun --cwd apps/desktop tauri build --no-bundle",
    "tauri:dev": "bun run wasm:build && bun --cwd apps/desktop tauri dev",
    "test": "cargo test --workspace",
    "test:browser-contract": "bun scripts/test-browser-contract.ts",
    "test:learned-provider-contract": "bun scripts/test-learned-provider-contract.ts",
    "test:video-sampling-contract": "bun scripts/test-video-sampling-contract.ts",
    "typecheck": "bun --cwd apps/web typecheck",
    "wasm:build": "bash scripts/build-wasm.sh",
    "wasm:check": "cargo check --target wasm32-unknown-unknown -p video-to-3d-wasm"
  }
}
